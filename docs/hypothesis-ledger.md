# 가설 원장 (Hypothesis Ledger)

목표($10/일 → $100/일)까지의 거리를 좁히기 위한 **가설-검증-폐기 루프의 단일 추적 문서**. 세션이 끝나도 가설이 사라지지 않게 하는 것이 이 문서의 존재 이유다.

## 왜 만들었나 — 지금까지 루프가 안 돌았다

기록에 남은 실패 패턴 5가지다. 이 문서의 규칙은 전부 이것들의 대응책이다.

| # | 실패 | 사례 |
|---:|---|---|
| 1 | **작은 n의 패턴을 룰로 박았다** | 표본 5건 전환율 표를 발행 승인 근거로 사용 → 2026-08-05 기각 |
| 2 | **빌려온 벤치마크를 기준으로 썼다** | 구글 CTR 통설을 네이버 하한으로 → 실측에서 정반대로 뒤집힘 |
| 3 | **생존 편향** | 이긴 글만 보고 공통점을 찾음. 진 글이 표에 한 행도 없었다 |
| 4 | **데이터를 본 뒤에 해석했다** | 네 스냅샷 연속으로 결론이 "방향은 위, 스케일은 1/160~200" |
| 5 | **가설이 배타적이지 않았다** | 여러 설명이 동시에 참일 수 있어 무엇을 죽여야 할지 알 수 없었다 |
| 6 | **지표가 재는 것을 확인하지 않았다** | 08-24 감사의 "1면 진입 9편" 이 전원 제목 역추정 키워드였다 → 2026-09-06 발각 |

## 루프의 형태

> **질문 하나 → 서로 배타적인 가설 2~3개 → 각 가설이 예측하는 관측값을 데이터 보기 전에 명시 → 관측 → 최소 하나를 죽인다**

핵심은 "맞았다"를 찾는 게 아니라 **"틀렸다"를 확정하는 것**이다. 가설이 배타적이면 하나가 죽을 때 나머지가 강해진다. 죽는 가설이 없으면 매번 같은 자리다.

## 규칙 (엄수)

1. **판정 기준은 데이터를 보기 전에 이 문서에 쓴다.** 사후에 쓴 기준은 무효로 취급한다
2. **n < 30이면 가설 생성에만 쓰고 판정 기준(임계값)으로 박지 않는다**
3. **이긴 것만 보는 분석은 시작하지 않는다.** 대조군(진 글)이 같은 표에 없으면 그 분석은 무효다
4. 각 가설에는 **반증 조건**과 **판정 예정일**이 반드시 붙는다. 둘 중 하나가 없으면 가설이 아니라 소감이다

---

## 🔴 살아있는 가설 — 검증 중

### 최상위 질문: 편당 클릭 상한이 왜 존재하는가

**2026-08-20 갱신 — 상한 값이 내려갔고 질문의 형태가 바뀌었다.**

30일 정규 스냅샷에서 1위가 **85클릭**이다. 91(08-05) → 94(08-10 환산) → 85, 세 시점 연속 80~90대다. 그런데 이번엔 더 강한 관측이 나왔다.

| 노출 | 클릭 |
|---:|---:|
| 3,176 | 22 |
| 2,096 | **35** |
| 1,065 | 30 |
| 424 | **31** |
| 363 | **31** |
| 274 | **85** ← 레딜, 유일한 예외 |

**노출이 7.5배 늘어도 클릭 최댓값이 31 → 35 다.** 노출이 커지는 만큼 CTR 이 깎여 곱이 상쇄된다. 레딜을 빼면 **사이트 편당 상한은 35** 이고, 그 값으로 $10/일 을 만들려면 2,857편(약 7년)이 필요하다.

→ 질문이 "상한이 왜 90인가"에서 **"노출을 키워도 클릭이 왜 안 느는가"** 로 바뀐다.

⚫ **2026-09-06 — 위 관측 전체가 기각됐다.** 같은 글(`brushcutter-blade-line-replacement`)이 노출 243 → **1,878**(7.7배)에 클릭 16 → **120**(7.5배), CTR 6.6% → 6.4% 로 유지했다. **노출을 키우면 클릭은 는다.** 상쇄가 일어난 것은 구조가 아니라 **CTR 이 낮은 자리에서만** 이다 — 09-06 기준 같은 노출대에서 계산기 0.7% / 꿈비 2.5% / 예초기 6.4% 로 갈린다. 위 표가 "노출 큰 글 = CTR 낮은 글" 만 담고 있었던 것이 문제였다(생존 편향의 거울상).

#### H2 (순위 상한) — ⚫ **사망 (2026-08-20)**

사전 반증 조건이 "노출 200+ 에서 CTR 10% 이상인 글이 나오면 죽는다"였고, `redill-zero-side-effects` 가 **274노출 / CTR 31.0%** 로 나왔다. 규칙 1(사전 기준을 사후에 고쳐 쓰지 않는다)에 따라 사망 처리한다.

#### H1 (자리 크기) — ⚫ **"400 위는 평평하다"는 부분은 사망 (2026-09-06)**

~~노출 400 까지는 클릭이 같이 늘지만 그 위에서 평평해진다.~~ 예초기가 **노출 1,878 에 클릭 120** 이다. 평평하지 않다.

남은 형태: **자리 크기는 클릭의 상한을 정하지만 하한은 CTR 이 정한다.** 노출이 커도 CTR 이 1% 대면 클릭은 안 늘고(계산기 3,176→22), CTR 이 6% 면 그대로 따라 는다(예초기 1,878→120). → **설명 변수는 자리 크기가 아니라 CTR 이고, 그건 H3 다.**

#### H3 (의도 불일치) — 🔴 **생존, 2026-09-06 에 강해졌다**

노출 300~599 구간에서 CTR 이 1.6~8.5% 로 5배 흩어진다. 같은 자리 크기에서 갈리므로 H3 예측과 맞다.

~~다만 노출 600 이상에서는 분산이 사라진다(아래 H4).~~ ⚫ **09-06 에 뒤집혔다** — 노출 1,000 이상에서도 분산이 그대로다(계산기 3,176→0.7% / 꿈비 1,486→2.5% / 예초기 1,878→**6.4%**). H4 가 죽으면서 **H3 이 모든 노출 구간에서 유효**해졌고, H1 의 잔여분까지 흡수한다.

---

### 새 가설 (2026-08-20 등록) — H4 는 2026-09-06 에 사망, H5 는 판정 예정일 2026-09-17

| | 가설 | 예측하는 관측 | 반증 조건 |
|---|---|---|---|
| ⚫ **H4** ~~살아있음~~ **사망 2026-09-06** | **노출 600 경계** — 노출 600 이상 자리에서는 우리 도메인이 구조적으로 하위 순위에만 들어가 CTR 이 3% 미만으로 고정된다 | 08-18 발행 head 2편(율무팩 32,240 / 로레알 19,290)이 노출 600 을 넘기면 CTR 3% 미만 | 노출 600+ 에서 CTR 5% 이상인 글이 나오면 죽는다 |
| **H5** | **흡수형** — 편당 클릭을 늘리는 유일한 경로는 큰 자리가 아니라 **미세 롱테일을 한 글에 몰아넣는 것**이다 | 증상·상황 질문 5개 이상을 흡수한 글이 노출 300 이하에서 CTR 15% 이상 | 흡수 설계 글의 CTR 이 기존 파이프라인 산출물(3~8%)과 같은 범위면 죽는다 |

**H4 와 H5 는 배타적이지 않다. 둘 다 참이면 처방은 "큰 자리를 포기하고 흡수에 집중"이다.** 둘 다 죽으면 편수·편집 레버가 전부 소진되고 채널 전환이 남는다.

**H5 의 근거 (n=2, 임계값 아님 — 규칙 2)**

레딜 페이지는 TOP30 에 잡힌 4개 키워드(목아픔·가래·가슴통증·목통증)가 **페이지 클릭의 48% 뿐**이고, 나머지 44클릭이 목록에 못 들어온 더 작은 질문에서 온다. 한예나 페이지는 4개 키워드가 **100%** 이고 꼬리가 없다. 전자는 확장되고 후자는 안 된다.

---

### ⚫ H7 (2026-08-24 등록) — **사망 2026-09-06** — 진입을 정하는 것은 문서수가 아니라 네이버 자사 슬롯 점유율이다

**⚫ 2026-09-06 사망.** `serp-audit --from=2026-08-24` → **1면 진입 0/9.** 판정 창(14일)에 든 3편(슬롯 40·36·30%)이 전원 1면 밖이고, 중심 예측("40% 이상이면 진입")이 40% 에서 직접 실패했다.

상단점 6편(42~50%)은 아직 3~10일차라 처음에는 유보하려 했으나, **게이트가 재는 변수 자체가 진입과 무관하다는 직접 실측**이 나와 성숙도와 무관하게 죽인다 — 예초기 관련 15개 키워드를 `/api/domains` 로 찍은 결과 **외부 슬롯이 85·83·75·70% 로 열린 자리에서도 우리는 전부 없었다.** 슬롯은 "자리가 비었는가"만 답하고 "우리가 딸 수 있는가"는 답하지 못한다.

→ 실험 문서의 사전 규칙 "50%·43% 도 실패 → 슬롯은 진입을 설명하지 못한다. 게이트 폐기하고 08-20 형태 축으로 회귀" 를 적용한다. 상세: [`snapshots/naver-search/2026-09-06.md`](./snapshots/naver-search/2026-09-06.md)

~~판정 예정일 2026-09-07 (1차) · 2026-09-21 (재확인)~~

| | 가설 | 예측하는 관측 | 반증 조건 |
|---|---|---|---|
| **H7** | 우리가 1면에 드는지는 검색량·문서수·경쟁률이 아니라, **1면에서 네이버 자사(blog·cafe·kin·in.naver)를 뺀 외부 슬롯이 얼마나 남는가**로 정해진다 | 외부 슬롯 40% 이상인 `김치냉장고보상판매`(3,590)는 rank 1~3 진입, 30%인 `메디올 새치염색약`(1,240)은 실패 | 메디올만 진입하면 죽는다. 둘 다 진입하면 경계가 30% 아래이므로 게이트를 완화한다 |

⭐ **2026-08-25 — 중간점 1편 추가.** `도미나스 기미크림`(슬롯 **36%**, 검색 35,560)을 같은 축에 발행했다. 40%·30% 두 점으로는 "경계가 있다/없다"까지만 알 수 있고, 사이 한 점을 넣으면 **경계 위치를 30~36 / 36~40 으로 좁힐 수 있다.** 판정 규칙 확장표: [`experiments/2026-08-24-slot-ratio-gate.md`](./experiments/2026-08-24-slot-ratio-gate.md)

⚠️ **도미나스는 검색량이 다른 둘의 10~29배다.** 08-24 감사에서 월검색 1,000 이상 13편이 전원 1면 밖이었으므로, **실패해도 슬롯 때문인지 검색량 구간 때문인지 못 가른다.** 진입에 성공할 때만 강한 신호로 취급할 것.

⚠️ **발행 결정 자체가 H6 의 실사례다.** 슬롯 36% 는 현행 게이트(40%)로는 거절인데, "검증 중인 기준으로 자르면 H6(사전 거절로 더 많이 잃는다)를 반복한다"는 이유로 사용자가 거절을 뒤집었다. **H7 이 살아나면 이 판단은 틀린 게 되고, H6 이 강해지면 맞는 게 된다** — 두 가설이 이 한 편에서 직접 부딪힌다.

**근거 (2026-08-24 전수 감사)**

발행글의 타겟 키워드를 `/api/domains` 로 전수 판정했다 (`scripts/pipeline/serp-audit.mjs`).

| | 1면 진입 | 1면 밖 |
|---|---:|---:|
| 월검색 1,000 이상 | **0편** | 13편 |
| 월검색 1,000 미만 | 9편 | 21편 |

- **월검색 1,000 이상에서 1면에 든 적이 한 번도 없다**
- 1면에 든 9편은 전부 월검색 10 이고, 키워드가 `아이팜 세면대는 수도 연결이 아닙니다` 같은 **우리 글 제목 그대로**다. 아무도 그렇게 검색하지 않는다
- **문서수·경쟁률이 무력하다** — `퍼시닌 바디워시` 월검색 38,230 / 문서 **423** / 경쟁률 0.01 인데 1면 밖. `이시언 샴푸` 31,720/542, `올레놀샷 NMN 포뮬러` 5,920/135 도 같다
- 반면 외부 슬롯 비율은 정렬됐다: **진입 성공 69%(한예나 rank3) · 41%(레딜 목아픔 rank2)** vs **실패 36·30·30·30·27·14%**

⚠️ **n=8(성공 2)이므로 40%는 임계값이 아니라 잠정 경계다** (규칙 2). 통과군·대조군을 같은 날 발행해 경계를 실측으로 고친다 → [`experiments/2026-08-24-slot-ratio-gate.md`](./experiments/2026-08-24-slot-ratio-gate.md)

⭐ **H7 은 H6 과 방향이 반대로 보이지만 충돌하지 않는다.** H6("우리 게이트는 사전 거절로 더 많이 잃는다")은 *검색량 하한*과 *조기 실측 기반 유형 거절*을 겨냥한 것이고, H7 은 그 자리에 **실측 기반 거절축**을 새로 세운다. 실제로 H7 게이트는 `레딜 목아픔`(검색 50, 슬롯 41%)을 **통과**시킨다 — 하한이 거절했던 사이트 1위 자산이다.

### 새 가설 (2026-09-06 등록)

**두 가설 다 판정 기준을 데이터 보기 전에 여기 박는다** (규칙 1).

| | 가설 | 예측하는 관측 | 반증 조건 | 판정 예정일 |
|---|---|---|---|---|
| **H8** (2026-09-06 정정 — 아래 정정 기록) | **진입은 그 키워드로 이미 쓰인 문서의 절대 수가 정한다.** 네이버 블로그가 1면을 도배해도 문서 총량이 수십 건이면 들어간다 | 문서 **100 미만**에서 진입 · 문서 **1,000 이상**에서 미진입 | 문서 **1,000 이상인 키워드에서 진입이 나오거나**, 문서 **100 미만인데 미진입**이 나오면 죽는다 | **2026-09-20** |
| **H9** | **시즌 상승기 2주 전 발행이 클릭을 만든다** | 9월 초 발행한 10~11월 피크 글이 11월 스냅샷 웹문서 TOP30 에 든다 | 그 글들이 11월 스냅샷 TOP30 에 **한 편도 못 들면 죽는다** | **2026-11-15** |

**H8 의 근거 — 실측 표 (n=3 성공 / 7 실패. 임계값 아님, 규칙 2)**

⛔ **정정 기록 (같은 날 2026-09-06).** H8 을 처음에 **"rank1~2 에 그 의도에 정확히 답하는 네이버 블로그가 있으면 ⛔"** 로 썼다. 그날 후보 13개에 적용해보니 전부 걸렸고, 자체 자산에 같은 잣대를 대자 **근거로 든 레딜·한예나를 스스로 거절했다.**

| 키워드 | rank1~2 | 문서수 | 우리 |
|---|---|---:|---|
| 애초기 줄통 교환방법 | **blog.dimad.kr** / `11st` | **~0** | ✅ **1위** |
| 레딜 목아픔 | `blog.naver` **×9** / **blog.dimad.kr** | **1** | ✅ **2위** |
| 한예나 앱테크 | `blog.naver` ×6 / `on.tech-money` | **23** | ✅ 3위 |
| 올레놀샷 NMN 포뮬러 | — | 135 | ❌ |
| 퍼시닌 바디워시 | — | 423 | ❌ |
| 이시언 샴푸 | — | 542 | ❌ |
| 나이키 지압 슬리퍼 | `blog.naver` ×6 / `cafe.naver` | 1,205 | ❌ |
| 46cm 바디워시 | `blog.naver` ×5 / `cafe.naver` ×2 | 1,235 | ❌ |
| 강아지 자두 급여 | `in.naver` / `blog.naver` **×11** | 5,497 | ❌ |
| 예초기 날 교체 | `blog.naver` ×6 / `cafe.naver` | 5,615 | ❌ |

**레딜 목아픔은 `blog.naver` 9개 위에서 우리가 2위다.** 네이버 블로그의 존재는 진입을 막지 못한다.
갈리는 것은 **문서수** 하나이고, 성공 **0 · 1 · 23** vs 실패 **135 이상**으로 겹치는 구간이 없다.

- 이것은 데이터에 맞춰 기준을 고친 게 아니라 **근거를 거절하는 자기모순을 고친 것**이다. 판정 전에 잡았으므로 09-20 판정은 새 문언으로 진행한다
- ⚠️ **"문서수 가설은 죽었다"(퍼시닌 423)와 충돌하지 않는다.** 죽은 것은 *423 을 "적다"고 본 버전*이다. 실제 경계는 **23~135 사이**로 훨씬 아래다
- ⛔ **문서수는 정렬 기준이지 거절 기준이 아니다.** 하드 컷을 걸면 H6(사전 거절로 더 많이 잃는다)를 반복한다. 계절 계수와 같은 취급이다

**⛔ H8 의 단서 — 저문서에는 두 종류가 있고 한쪽만 유효하다 (2026-09-06 추가)**

문서수를 오름차순으로 정렬하자마자 **문서 93 짜리 `쿠쿠압력밥솥 가품`** 이 1순위로 올라왔다. 우리 승리 구간(0~23)에 가장 가까운 값인데 `/api/judge` 가 ⛔ 를 냈다 — `enuri.com`·`coupang.com` 이 43% 를 먹고 있다.

| 문서가 적은 이유 | 예 | 진입 |
|---|---|---|
| **아무도 안 썼다** (정보 진공) | 레딜 목아픔(1) · 한예나 앱테크(23) | ✅ |
| **커머스가 먹어서 블로그가 안 쌓였다** | 쿠쿠압력밥솥 가품(93) · 세포랩 블랙라벨(289) · 퍼시닌 바디워시(423) | ❌ |

**문서수만 낮게 정렬하면 후자를 계속 물어온다.** 이미 문서화된 반복 패턴이다 — 세포랩(문서 289 가 얇았던 이유가 홈쇼핑 편성표) · 포카리스웨트 340 무라벨 · 예초기 브랜드축 · 농약분무기 축.

→ **정렬은 문서수로 하되, 채택 전에 `/api/domains` rank1~2 에 커머스가 있는지 본다.** 이건 폐기된 슬롯 게이트가 아니라 **살아있는 의도 게이트**(쇼핑 SERP)다. 08-20 에 중단한 것은 *검색량 하한*과 *조기 실측 기반 유형 거절*이지 쇼핑 SERP 실측이 아니다.

⚠️ **`퍼시닌 바디워시`(423)가 이 분류로 재해석된다.** 지금까지 "문서 423 인데 1면 밖" 을 문서수 가설의 반례로 썼는데, 바디워시는 리뷰 블로그와 커머스가 그 의도에 답하는 자리다. **문서수가 낮아서 뚫릴 자리가 아니라 애초에 후자 유형**이었을 수 있다.

**⭐ 부수 발견 — 추천기는 우리가 이기는 구간을 구조적으로 못 보여준다**

| | 문서수 |
|---|---:|
| 우리 1면 진입 3건 | **0 ~ 23** |
| 2026-09-04 추천 10건 | **1,235 ~ 483,936** |

최소 50배, 최대 2만 배다. 추천기·`scan-seed` 가 **기회점수·검색량 순으로 정렬**하는데 문서 수십 건짜리 키워드는 검색량도 작아서(한예나 2,000 / 레딜 목아픔 **50**) 목록 바닥에 깔린다. **0/22 의 구조적 원인이 이것으로 보인다.**

→ ✅ **반영: `scan-seed.js` 의 judge 후보 정렬을 문서수 오름차순으로 변경** (2026-09-06).

**H9 의 근거 (n=1 — 관측이지 룰이 아니다)**

`brushcutter-blade-line-replacement` 08-03 발행 → 2주 뒤 노출 → 9월 벌초 피크 탑승.
`/api/trend` 실측: `예초기` 지수가 9월 **100** / 10월 24.6 / 11월 8.4 / 12월 6.0 이고 8+9월이 연간의 약 45%.
클릭 16(08-20) → **120**(09-06).

⚠️ **그래서 120 은 지속 수치가 아니다.** 10월에 1/4 로 내려간다. H9 는 "120 이 유지된다" 가 아니라
**"상승기에 미리 올라타면 그 시즌을 먹는다"** 이고, 이건 매년 반복 가능한 자산이다.

---

### 판정 절차 자체에 대한 가설 (2026-08-20 등록)

| | 가설 | 근거 | 검증 방법 |
|---|---|---|---|
| **H6** | **우리 게이트는 발굴 실패보다 사전 거절로 더 많이 잃는다** | 사이트 1위(레딜 목아픔 — ⛔ 하한 미달) 와 2위(한예나 — ⛔ 의도 불일치) 가 **둘 다 자체 게이트가 거절한 키워드**다. 둘 다 사용자가 게이트를 무시하고 발행시켰다 | 검색량 하한 단독 거절과 30일 이내 실측 기반 유형 거절을 **중단**하고, 다음 코호트에서 거절했을 후보의 성적을 추적 |

⚠️ H6 은 "게이트를 다 없애라"가 아니다. **거절 근거로 쓰던 두 축(검색량 하한 · 조기 실측)이 실패한 것**이고, SERP 실측(`/api/judge`)은 여전히 유효하다.

---

## ⚪ 검증 대기 — 표본이 안 익었다

| 가설 | 필요한 것 | 예정일 |
|---|---|---|
| ~~H1/H3 최종 판정~~ | ~~08-24 코호트~~ | ⛔ **2026-08-24 판정 불가 — 아래 참조** |
| 서브 키워드를 H2로 흡수하면 부분매칭으로 걸리는가 | 08-03·08-04·08-10 발행분 측정 | 2026-08-24 |
| ~~H7 (슬롯 비율)~~ | ⚫ **2026-09-06 사망** | — |
| ~~H4 (노출 600 경계)~~ | ⚫ **2026-09-06 사망** | — |
| **H8 (진입 조건 = 문서 절대 수)** | 문서 100 미만 후보를 발행하고 14일 뒤 대조 | **2026-09-20** |
| **H9 (시즌 선점)** | 10~11월 피크 글을 9월 초 발행 | **2026-11-15** |
| H5 (흡수형) | 흡수 설계 글 1~2편 발행 후 30일 | 미착수 — 발행이 선행 조건 |
| 시즌 하강 구간 진입이 다음 시즌의 창인가 (하스카프) | 가을 식재 시즌 관측 | 2026-09말·10말 |

### ⛔ 2026-08-24 — H1/H3 코호트 판정이 무산됐다 (절차 실패 기록)

판정 예정일에 GA4 전수 데이터(28일·90일)와 코호트 43편을 확보했는데도 **판정할 수 없었다. 이 문서에 H1/H3 의 임계값이 사전에 적혀 있지 않았기 때문이다.** "코호트 25~30편으로 판정" 이라고만 있고 "얼마면 죽는다"가 없었다.

규칙 1(판정 기준은 데이터를 보기 전에 쓴다)에 따라 지금 기준을 만들면 무효다. 이것이 **실패 패턴 4번(데이터를 본 뒤에 해석했다)** 그 자체이므로, 사후 기준을 만드는 대신 판정 불가로 기록한다.

**다만 판정 없이 확정된 사실은 있다 (산수이지 해석이 아니다).**

- 코호트 43편의 28일(07-27~08-23) 총 조회 **207** · 편당 4.8 · 수익 **₩1,562**
- 성숙분(08-11 이전 발행 35편)만 잘라도 편당 5.4 · ₩1,449 로 결론이 안 바뀐다
- **글 나이 효과는 없다** — 90일 창에서 일평균으로 환산하면 나이대별 0.19~0.36 조회/일로 평평하다. 28일 창에서 "0~30일 5.4 → 31~60일 9.4" 로 보였던 것은 신규 글이 창에 절반만 존재해 생긴 **산술 착시**였다
- **히트(90일 조회 90+)는 7편 = 3.4%** 이고 조회의 32%를 차지한다. **07-17 이후 발행 43편 중 히트는 0편**
- **RPM 레버는 없다** — 조회 구간별 가중 RPM 이 $2.22 / $2.42 / $3.15 / $2.92 로 평평하다. 저조회 글의 높은 RPM(올레놀샷 $27.5)은 광고 클릭 1건이 만든 우연이며 규칙 2에 걸린다
- Direct 채널(사용자 40.6%)이 수익의 **1.7%** 뿐이다(사용자당 ₩0.2 vs 오가닉 ₩7.0). Direct 를 뺀 실제 오가닉 RPM 은 **$4.54** 로 한국어 실측 중앙값 $2.87보다 위다 → **08-05 결론(병목은 수익화 구조가 아니라 트래픽) 재확인**

→ **다음 코호트 판정을 위해 H7 의 임계값을 이번에는 사전에 못 박았다** (위 H7 항목).

### ⛔ 2026-09-06 — 지표가 재던 것이 가짜였다 (절차 실패 기록)

08-24 전수 감사에서 **"1면 진입 9편"** 을 기록했고, 그것을 근거로 *"1면에 들어도 검색량이 10 이라 클릭이 0"* 이라고 해석했다. 그 해석이 "1면 진입은 목표가 될 수 없다"는 결론까지 이어졌다.

09-06 에 그 9편의 키워드를 다시 열어봤다. **전원 `exact=undefined`** — `targetKeyword` 가 frontmatter 에 없어서 `serp-audit` 이 **글 제목에서 역추정한 문자열**이었다.

```
rank1  "아이팜 세면대는 수도 연결이 아닙니다"
rank2  "워싱소다 vs 베이킹소다 vs 과탄산소다 차이 - 성분·pH·용도로 구분"
rank1  "메디톨드 바디워시 어디서 사야 싼가"
```

아무도 이렇게 검색하지 않는다. **1면에 들었는데 클릭이 0 이었던 게 아니라, 1면에 든 적이 없었다.**

- **실검색어 기준으로 확인된 1면 진입은 3개뿐이다** — 레딜 목아픔(2위) · 한예나 앱테크(3위) · 애초기 줄통 교환방법(1위). **그중 2개가 사이트 역대 1·2위 페이지다**
- ✅ **반영**: `scripts/pipeline/serp-audit.mjs` 가 진입률을 `targetKeyword` 보유분으로만 계산한다. 역추정분은 `~` 표시 + 분모 제외 + 경고 출력
- 실패 패턴 6번으로 등록했다. **지표를 만들 때 "이게 실제로 무엇을 재는가"를 한 번은 열어볼 것**

### ⭐ 1차 지표를 "1면 진입"으로 전환한다 (2026-09-06)

| 근거 | |
|---|---|
| 클릭은 지금 측정이 안 된다 | 161편이 편당 월 **0.68클릭**. 0.68 → 1.2 를 개선으로 판정할 수 없다 |
| 1면 진입은 이진값이고 **14일이면 판정**된다 | 클릭 성숙(30일+)의 절반 |
| 개선 여지가 100% | 실검색어 기준 확인된 진입이 **3개** |
| 실측 배수 | 1면 밖 6.4% → 1면 1위 15.8% → 1면 2위 **68.4%** |

🔒 **`targetKeyword` 는 발행 전에 frontmatter 에 박고 사후에 바꾸지 않는다.** 안 그러면 쉬운 키워드로 갈아타 진입률을 올릴 수 있다 (지표 해킹). git 이력으로 강제된다.

⚠️ **"1면에 들면 이긴다"는 뜻이 아니다.** `한예나 앱테크` 는 **3위인데 CTR 2.5%** 다 — 의도가 안 맞으면 1면에서도 안 눌린다(H3). **측정 가능한 유일한 축이라서 1차 목표로 쓰는 것**이고, 편당 월 클릭이 5 이상으로 올라오면 지표를 다시 연다.

## ⚫ 죽은 가설

새로 죽는 가설은 여기에 **언제·무엇으로 죽었는지**와 함께 기록한다. 2026-08-10 이전에 폐기된 가설들은 [`content-strategy.md`](./content-strategy.md) "폐기된 가설" 섹션과 [`snapshots/naver-search/2026-08-05.md`](./snapshots/naver-search/2026-08-05.md) "기각된 가설 목록"에 있다. 주요 항목:

| 가설 | 사망일 | 사인 |
|---|---|---|
| **H7 — 슬롯 비율이 진입을 정한다** | **2026-09-06** | `serp-audit` 0/9. 판정 창에 든 3편(40·36·30%) 전원 1면 밖. 결정타는 예초기 15콜 — **외부 슬롯 85·83·75·70% 인 자리에서도 우리가 전부 없었다** |
| **H4 — 노출 600 경계** | **2026-09-06** | 사전 반증 조건 "노출 600+ 에서 CTR 5% 이상" 에 예초기 1,878노출/6.4% 가 걸렸다 |
| **편당 상한 35 · "노출 400 위는 평평하다"** | **2026-09-06** | 예초기가 노출 7.7배(243→1,878)에 클릭 **7.5배**(16→120), CTR 유지. 08-20 관측의 정확한 반대 |
| **H2 — 순위 상한** (도메인 권위가 낮아 큰 자리에서 하위 순위로만 노출된다) | **2026-08-20** | 사전 반증 조건 "노출 200+ 에서 CTR 10% 이상" 에 `redill-zero-side-effects` 274노출/31.0% 가 걸렸다 |
| **레딜의 승리 키워드는 "구입처"다** (2026-08-05 정정) | **2026-08-20** | 3개월 뒤 구입처 827 → TOP30 밖(-99%), 부작용 91 → 85(-7%). **크기만 보고 지속성을 안 봤다** — 휘발한 쪽이 구입처였다 |
| **레딜형 재현 = 구매 의도 × 커머스 부재** | **2026-08-20** | 위 정정과 함께 전제가 무너짐. 재현 대상은 827짜리 스파이크가 아니라 85짜리 에버그린 |
| **큰 자리로 옮기면 클릭이 는다** (H1 강한 버전) | **2026-08-20** | 노출 424→3,176(7.5배)인데 클릭 31→22. 노출 400 이상에서 평평해진다 |
| 발행 하한 산식을 **승인 근거**로 쓸 수 있다 | 2026-08-05 | 표본 5건이 전원 월검색 5~120짜리 **1면 진입 승자**. 진입 확률이 곱해져 있지 않은 생존 편향 (거절 근거로는 계속 사용) |
| 내부링크 보강이 구글 색인을 푼다 | 2026-08-05 | 유일한 통제 실험에서 null. 미색인 109 중 82가 "발견됨-미크롤" — 크롤 **수요** 문제라 편집으로 안 풀림 |
| IndexNow 가 구글 색인에 도움된다 | 2026-08-05 | Bing·Naver 전용. 구글에 무효 |
| 계산기 title 수리로 CTR이 오른다 | 2026-08-05 | 0.5% → 0.24%. 수리 실패 확정 |
| `/api/trend` 로 시드를 발굴할 수 있다 | 2026-07-17 | 엔드포인트 6개 전부 "키워드를 이미 알 때" 쓰는 도구. 시드는 도구 밖에서 와야 함 |
| 구글 CTR 통설을 네이버 하한으로 쓸 수 있다 | — | 실측에서 정반대로 뒤집힘 |

---

## 갱신 규칙

- **가설이 죽거나 태어날 때마다 즉시 갱신한다.** 스냅샷 리뷰·실험 측정 워크플로의 마지막 단계에 이 문서 갱신을 포함할 것
- 살아있는 가설이 **판정 예정일을 넘겼는데 판정되지 않았으면** 그 자체를 문제로 다룬다. 방치된 가설이 쌓이면 이 문서도 소감집이 된다
